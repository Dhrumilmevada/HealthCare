<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rel_lay_reset_password_continue"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.dhrumil.healthcare.resetPassword.ResetPasswordContinue">

    <include
        layout="@layout/reset_toolbar_layout"
        android:id="@+id/toolbar_reset_password_cont">
    </include>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/frame_reset_password_continue"
        android:layout_below="@+id/toolbar_reset_password_cont">
    </FrameLayout>
</RelativeLayout>
