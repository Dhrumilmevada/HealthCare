<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/rel_lay_email_frag"
    tools:context="com.example.dhrumil.healthcare.resetPassword.fragment.EmailFragment">


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edt_email_id"
        android:hint="@string/enter_email_id"
        android:inputType="textEmailAddress"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_submit"
        android:layout_below="@id/edt_email_id"
        android:text="@string/submit"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_cancel"
        android:layout_toEndOf="@id/btn_submit"
        android:layout_alignTop="@id/btn_submit"
        android:layout_marginStart="10dp"
        android:text="@string/cancel"/>
</RelativeLayout>
